spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todo_manager
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  devtools:
    livereload:
        enabled: true
server:
  port: 8000

